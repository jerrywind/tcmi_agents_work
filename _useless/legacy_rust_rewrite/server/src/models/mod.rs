//! 领域数据模型（阶段 A 占位）。
//!
//! 后续阶段将在此定义与 `backend/app/models/schemas.py` 完全对齐的 Rust 结构
//! （`Patient` / `Family` / `Member` / `Consultation` / `Evidence` / `Report` 等），
//! 采用 serde 序列化，字段名/类型需与前端契约一致。

// 占位：后续阶段补全。
