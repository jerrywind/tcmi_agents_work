# Docker 构建上下文排除表（context = server/，即 Cargo workspace 根）
#
# 镜像**在 Docker 内编译**，因此源码必须进入上下文；
# 只需排除庞大的构建产物与版本库元数据。
# 依赖未变动时，Dockerfile 里的「清单 + 占位源码预编译依赖」层会命中缓存。

# Rust 构建产物（常达数 GB，且镜像内会重新编译，必须排除）
**/target/

# 版本库元数据
.git/
.gitignore

# 编辑器 / 系统噪声
.vscode/
.idea/
**/.DS_Store
