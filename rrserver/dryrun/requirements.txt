# 联调客户端 (backend_via_tunnel.py) 的最小第三方依赖。
# 它通过 sys.path 注入 backend/ 并 import app.protocol.llm，其真实依赖链为：
#   app/protocol/llm.py -> httpx
#   app/config.py       -> PyYAML
# 不需要 uvicorn/fastapi 等服务端依赖（uvicorn[standard] 含 uvloop，Windows 无法安装）。
httpx>=0.27
PyYAML>=6.0
pydantic>=2.0
