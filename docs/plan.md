# 项目计划与路线图（Plan & Roadmap）

> 基于 2026-08-29 的全项目审视重写。结论：**后端骨架已成形，但「前端 ↔ 后端」这条链路
> 目前是断的，且技能体系完全没有接入推理链路**。下一阶段的重心是「打通 + 提质」。
> 任务清单见 [`tasks.md`](./tasks.md)。

---

## 0. 现状概览

> 2026-08-30 更新：已完成四轮——「打通端到端 + 修复已知 bug」、
> 「技能与工具生效 + 健壮性与可观测」、「诊疗提质」，以及本轮
> 「语料建设 + 生产化」（T4.3 RAG 语料与召回评估、T5.1 报告持久化、T5.4 合规；
> T5.2 / T5.3 出检查清单）。
> 阻断可用的 P0 问题已全部解决；剩余工作要么是**内容建设**（语料/评测扩样），
> 要么**依赖外部资源**（云存储、域名备案、小程序资质）。

| 模块 | 状态 | 说明 |
|---|---|---|
| `server/harness/` 编排 | ✅ 可用 | 13 个 Sub-Agent + `routing.yaml` 驱动的串行编排；单步失败已可降级；安全门固定前置 |
| `server/harness/resources/*.yaml` | ✅ 可用 | 流程与数据分离，中医人员可维护 |
| `server/harness/tests/cases.rs` | ⚠️ 护栏，非基准 | 以 `cases.jsonl` 校验资源完整性（不依赖 LLM）。**它是合成数据**：93 条只有 5 种主诉、3 种证候组合，37 条主诉为占位符 `x`；断言也很宽松（候选集覆盖即可）。守护「数据没写漏」有效，衡量「辨证准不准」无效 |
| `server/rrserver/` | ✅ 可用 | 云端中继 + 家庭端 client + 模型部署包装；资源泄漏已修 |
| `llm_server/` | ✅ 可用 | LM Studio 网关 + Agent 中间层；`rag/` 为可选子组件 |
| `deploy/` | ✅ 可用 | nginx 统一入口 + compose 编排 |
| **前端 ↔ harness 链路** | ✅ **已打通** | 旧 `api.ts` 已删除，6 个页面改用 `harness.ts` + `session.ts` 维护多轮 |
| **技能 → 推理链路** | ✅ **已接线** | 13 个 Agent 全部走 `chat_with_tools`（T2.0 时是 7 个，后随流程拆分增补） |
| **后端构建** | ✅ **Docker 化** | 两个 Dockerfile 改为多阶段、镜像内编译，不再依赖 WSL2 预编译 |
| **MCP Client** | ✅ 已接线 | `config.yaml` 的 `mcp_clients` 启动时挂载为 `mcp__*` 工具（T2.4） |
| **MCP Server** | ✅ 已具备 | `POST /mcp` 对外暴露 7 个 `agent_*` 工具 + `run_agent` + `list_agent_capabilities`（T4.5） |
| **可观测性** | ✅ 已具备 | `/chat` 与 `POST /agents` 逐步返回 `trace`（耗时/token/模型/工具/错误，T3.1） |
| **结构化辨证** | ✅ 已具备 | 辨证步产出主证/兼证 + 置信度 + 支持/矛盾证据，随 `structured` 返回（T4.1 / T4.2） |
| **证候锁定** | ✅ 已具备 | 辨证主证注入治疗期各步，立法/用药/开方与辨证同源，不再各自重猜（T7.1） |
| **治疗期工具** | ✅ 已具备 | 方剂/调护检索对用药·开方·调护可见，owner 支持多归属（T7.2） |
| **人群适配问诊** | ✅ 已具备 | 问诊按 `payload.gender` 过滤，男患者不再被追问月经（T7.3） |
| **方剂库** | ✅ 已具备 | 19 首经典方覆盖全部 6 个证候，带出处；开方输出与库记载做药味比对（T7.4） |
| **规则结论喂给模型** | ✅ 已具备 | 候选方剂/药物/治则注入 system（而非拼在输出末尾），模型按库载方剂开方（T7.6） |
| **质量评测** | ⚠️ nightly | `tests/llm_eval.rs` 自动评分（需真实 LLM，不进 PR 门禁，T4.4） |
| **RAG 语料** | ✅ 已具备 | 700 部中医典籍 / 6618 万字建索引（60s），`rag/eval/` 24 条人工样例，基线 hit@5 95.8%（T4.3） |
| **报告持久化** | ✅ 已具备（默认关） | `GET /reports`、`GET /reports/:id`，落盘脱敏，配 `HARNESS_STORE_DIR` 才启用（T5.1） |
| **合规底线** | ✅ 已具备 | 免责声明随结果下发、安全门不可被配置移除、落盘脱敏（T5.4） |
| **真实 LLM 验收** | ✅ **已通过** | 3 个用例跑通（含红旗拦截），样例归档 `docs/samples/`；内容「可用但需医师把关」 |
| **症状分级（主症/次症）** | ✅ 已具备 | 主症权重 1.0、次症 0.4，凑次症压不过命中主症（H2） |
| **主症必备 + 孤证不立** | ✅ 已具备 | 至少命中 1 条主症且证据量 ≥ 1.5 才出主证，否则如实说「不知道」（H3） |
| **库外诚实降级** | ✅ 已具备 | 无合格候选时 `primary=None` + `near`（最接近谁、缺哪条主症），不硬塞库内证（H3） |
| **证候锁定门槛** | ✅ 已具备 | 置信度 < 0.4 不锁定；响应带 `low_confidence` / `confidence_note`，正文置顶（H4） |
| **结论可信度可见** | ✅ 已具备 | 未定证 / 置信度不足 / 强制放行三种情形均写入正文与响应，此前全部静默（H4/H5） |
| **黄金病例集** | ✅ 已具备 | `golden_cases.jsonl` 21 条，断言首位命中 + 库外不得出证（H7） |
| **治疗期全步感知不确定性** | ✅ 已具备 | 立法 / 用药 / 开方 / 调护 /**针灸** / 综合治疗六步全覆盖（H6 + I1） |
| **未定证时的可行动提示** | ✅ 已具备 | 前端展示「最接近谁、还缺哪条主症」，用户知道该补什么（I3） |
| **H5 端可用** | ✅ 已具备 | 修掉模块顶层 `process.env` 导致的白屏、请求超时过短、提示条 Markdown 星号（J1–J3，真机验证） |
| **LLM 评测基线** | ✅ 已具备 | 以黄金病例集双向评分，规则层与正文层分开报告（J4，首份基线 1.0） |

**一句话**：端到端可用（已在真实模型上跑通并人工验收）、工具真正生效、调用可观测、
辨证结论结构化且能锁定到治疗期、能力可对外暴露、典籍检索有真实语料、报告可存证回查；
**辨证结论的可信度是可见的**——系统知道自己不知道，会把这句话传到报告里（阶段 H），
并告诉用户「还差哪些表现就能定证」（I3）；治疗期六个步骤全都能感知证候是否可信（H6 + I1）；
测试全绿（后端 216 / 前端 46 / RAG 12+6）、构建完全 Docker 化；
H5 端真机可用（白屏、超时、提示条星号三处已修，均为真机才暴露的问题）；
下一步重心是**知识厚度**（证候库 16 个，真实中医数以百计，且新增内容须医师复核）
与外部资源到位后上线。
（后端 212 = harness 65 + rrserver 147；前端 36 含 6 条契约）

---

## 1. 问题清单

### ✅ 本轮已修复（2026-08-29）

1. **前端契约未对齐** —— 删除 `services/api.ts`（其所有端点在 harness 中不存在），
   5 个页面改接 `harness.ts`，新增 `services/session.ts` 维护多轮 `messages`。
2. **前端 18 个用例失败** —— 根因是 `vitest.setup.ts` 的 mock 返回普通 async 函数
   而非 `vi.fn()`（`mockImplementation is not a function`）；改为 `vi.hoisted()` +
   `vi.fn()`。端口从废弃的 `:22000` 改为 `:8011`。现 21/21 全绿。
3. **无多轮问诊** —— 由前端 `session.ts` 累积 messages 后重复调用 `/chat`。
4. **工具调用链路断开** —— 7 个 Agent 全部改用 `chat_with_tools`，技能真正进入推理。
   （当时只有 7 个 Agent；流程拆分后增至 13 个，均沿用同一链路。）
5. **RAG 契约不匹配** —— `tcm-rag` 支持 `top_k`，数组响应统一包成 `{"result": [...]}`。
6. **`/chat` 无降级** —— 改为部分成功返回（`steps` + `failures` + `partial`）。
7. **死代码 / 无效配置** —— `model.rs` 删除 5 个未使用类型；`routing.yaml` 未读取的
   `default` 已移除。
8. **rrserver 资源泄漏（新发现）** —— `ChunkStream::poll_next` 里
   `let _ = reg.cancel_pending(..)` 只创建 future 随即丢弃，清理**从未执行**，
   `pending`/`streams` 持续累积。改用同步的 `cleanup_pending_sync` /
   `cleanup_stream_sync`（`try_lock`）。
9. **清单顺序随机（新发现）** —— `HashMap` 迭代顺序不稳定，
   `GET /agents` / `GET /skills` 每次启动顺序都可能变。
   现 `Registry::capabilities()` 按规范顺序、`SkillRegistry::all()` 按名排序。
10. **CI 失效** —— 重写为 Docker 化的后端测试/lint/镜像构建 + 前端测试 + 真实后端契约测试。
11. **后端构建不符合 Docker 铁律** —— 两个 Dockerfile 从「COPY WSL2 预编译二进制」
    改为多阶段镜像内编译；`build-release.ps1` 相应简化。

### ✅ 本轮已解决（2026-08-29，第三轮）

12. **MCP Server 未做** —— 新增 `POST /mcp`（`src/mcp/server.rs`），对外暴露
   7 个 `agent_*` 工具 + `run_agent` + `list_agent_capabilities`；
   `tools/call` 直接翻译成 `run_single`，不改编排器。
13. **无自动化 LLM 回归** —— 新增 `tests/llm_eval.rs`：以 `cases.jsonl` 为基准
   跑真实辨证并自动评分（按语料去重、单条超时、产出 JSON 报告），
   nightly 工作流跑分，默认不启用以免影响 PR 门禁。
14. **辨证未结构化** —— 辨证结论升级为结构化对象（主证 / 兼证 / 置信度 /
   支持证据 / 矛盾证据 / 病机 / 传变），经 `structured` 字段返回；
   矛盾证据由新增的 `resources/contradictions.yaml`（相反表现表）计算。

### ✅ 本轮已解决（2026-08-30，第四轮）

15. **无 RAG 语料** —— `llm_server/rag/corpus.py` 落地中文典籍检索：
    按 `<篇名>` 切条目、剥离书目元数据头、短条目标题拼进正文，
    bigram 倒排 + BM25 书级召回 + 片段级重排（整词加权、同书限流、泛词过滤）。
    实测 700 部 / 6618 万字建库 60s，查询平均 41ms。
    评估集 `rag/eval/tcm_queries.jsonl` 24 条 + `eval_rag.py`，
    基线 hit@5 95.8% / MRR 0.958。
16. **无报告持久化** —— `src/store.rs` + `GET /reports`、`:id`；
    **默认关闭**以保持 harness 无状态，配 `HARNESS_STORE_DIR` 才启用。
17. **合规无保障** —— 免责声明随每份结果下发（前端必须展示）；
    安全门不可从 `routing.yaml` 移除（缺失则强制插入并告警）；
    落盘前脱敏强标识信息，临床数字保留。

### 🔲 仍待处理

| # | 问题 | 任务 | 阻塞原因 |
|---|---|---|---|
| 18 | ~~未连真实 LLM 做过人工验收~~ | T1.5 | ✅ 已完成：3 个用例跑通并归档，审阅发现 4 个待改进项（见下） |
| 19 | **无对象存储**：图片不落盘，报告无附图 | T5.2 | 需云资源；清单已出 |
| 20 | **未上 CDN / 小程序未过审** | T5.3 | 需域名备案 + 医疗类目资质；清单已出 |
| 21 | **评测基线未建立**：`llm_eval` 需真实 LLM 跑一次才有基线分 | T4.4 | 可跑（LLM 已通）；跑一次即可定门槛 |
| 22 | **证候库太薄 = 系统能力上限**：`syndromes.yaml` 已从 6 个扩到 16 个（H8），真实中医证候数百，库外的证 `assess()` 仍辨不出来 | H8 | **仍需执业中医师复核与继续扩容**（症状/舌脉/病机/治则 + 方剂组成与剂量）。H3 已让库外情形**诚实降级**而非硬塞，但那对患者仍不是答案。**这与 `cases.jsonl` 无关**——那份合成基准只覆盖 3 个证候且全在库内，永远不会暴露这个缺口 |
| 24 | **回归基准是合成的**：`cases.jsonl` 93 条仅 5 种主诉（37 条主诉为 `x`）、3 种证候组合、且标签自相矛盾；`cases.rs` 只断言「候选集覆盖」 | H7 部分缓解 | `golden_cases.jsonl` 补上了「首位命中」与「库外不得出证」，但它同样是人工构造的教科书式病例。仍需真实/专家标注的病例集 |
| 25 | **LLM 评测以合成基准为输入**：`llm_eval.rs` 沿用同一份 `cases.jsonl`，分数高不代表真实场景可信 | 新增 | 建议改用 `golden_cases.jsonl` 作输入（至少它断言首位命中） |
| 26 | ~~**前端未展示 `near`**~~ | I3 | ✅ 已解决：consult 页与 report 页均渲染「补充这些表现可能就能定证」，逻辑抽成纯函数并配 5 条单测 |
| 27 | **方剂库无剂量**：`formulas.yaml` 只有组成与用法，模型会自行生成剂量数字 | 阶段 J 部分缓解 | 剂量约束已铺到 `prescription` / `herbology` / `treatment` / `acupuncture` 四步提示词；彻底解决须补剂量字段（专业复核） |
| 30 | **小程序端跑不完完整问诊**：`/chat` 串行 10 步需 200–530s，`wx.request` 超时上限约 60s | J2 暴露 | 把 `/chat` 改成分步请求或「提交任务 + 轮询结果」——后端接口形态改造 |
| 28 | **契约测试在 CI 里等于不存在**：I4 加了超时与 WARN 让它不再静默，但它默认连 `127.0.0.1:8011`，CI 无 harness 仍会跳过那 6 条 | I4 部分缓解 | 在 CI 里起 harness 容器并等待就绪，或在 compose 中编排前端测试与 harness |
| 29 | **组件单测仍为 0**：阶段 J 用临时 Playwright 做了 H5 真机验证（修掉白屏/超时/星号三处），但未常态化 | 阶段 J 部分缓解 | 引入 `@playwright/test` 配一条 e2e；小程序端仍未验证过 |
| 23 | ~~**RAG 单机部署不可达**~~ | T7.5 | ✅ 已解决（2026-09-01：`/health` 暴露 `rag.*` 可达性，`tcm-rag` 降级文案改为明确的 error） |

### ⚠️ 真实 LLM 验收发现的待改进项（T1.5 产出）

自动化测不出、只有人看输出才会发现的问题，按优先级排：

1. **方剂与主证不对口**：主证判「脾胃湿热（中焦）」，治疗却开「龙胆泻肝汤」（清肝胆湿热），
   且同一份报告的切诊步说的是「半夏泻心汤」——前后不一致。
2. **经方药味记错**：麻黄汤写成「麻黄、桂枝、杏仁、**白术**、甘草」（原方无白术）。
   1、2 的根治办法是**用 `formulas.yaml` 约束方剂输出**，而非靠提示词。
3. **问诊未读性别**：`payload.gender=男` 仍追问月经。
4. **技能调用偏少**：7 步中只有切诊触发 `tcm-palpation`，望/闻/问/辨证/治疗都没调用工具
   （`max_tool_rounds=3` 的循环没被模型用起来）。

### ✅ 已于 2026-09-01 阶段 G（T7.1–T7.6）全部解决，并完成真实 LLM 复验

> 这四条都不影响「链路可用」，但决定了「结论能不能信」。
> 定位后发现：根因**不在提示词，而在架构与知识库**——
> 靠提示词硬掰压不住模型幻觉，得改结构、补数据。详见 [`tasks.md`](./tasks.md) 阶段 G。
>
> **复验结论**（`google/gemma-4-12b-qat`，`run_manual_e2e.ps1`）见 tasks.md
> 「阶段 G 真实 LLM 验收」：四条意见逐条消除。
> 其中 **T7.6 是复验才逼出来的**——数据、工具都已到位，但规则结果拼在
> LLM 输出**末尾**，模型生成时看不到，于是自造方名；把结果移进 system 后
> 主方立刻改为库载方剂。教训：**改完不算完，必须在真实链路上看一眼输出**。

1. **方剂与主证不对口** —— 根因是辨证结论**不下传**：治疗期各步各自用
   `infer_syndrome_slug`（关键词计数）从原始对话重新猜证候，与辨证步的
   `assess()` 不是同一套算法。故做「证候锁定」（T7.1），并把方剂库从
   7 首扩到 19 首、脾胃湿热补上三仁汤/连朴饮（T7.4），不再需要模型现编。
2. **经方药味记错** —— 麻黄汤根本不在库里，模型只能凭记忆生成。
   现已入库，并在开方步加了确定性的药味比对（T7.4）。
3. **问诊未读性别** —— `inquiry.rs` 的 payload 参数名是 `_payload`，
   前端传了性别但从未读取。现已按 `payload.gender` 过滤并把性别告知模型（T7.3）。
4. **技能调用偏少** —— 配置层 bug：`tcm-formula`/`tcm-care` 的 owner 是
   `Treatment`，而默认 `standard` 档跑的是「立法→用药→开方」，
   这三步一件专属工具都拿不到。owner 已改为集合（T7.2）。

---

## 2. 阶段性路线图

### 阶段 A：打通（1–2 周）—— 让「一次完整问诊」真的能跑通
目标：**有一条用户可用的链路**。这是所有后续工作的前提。

- A1 前端契约对齐：5 个页面切到 `services/harness.ts`，`api.ts` 下线。
- A2 前端维护多轮 `messages`，实现「追问 → 回答 → 再追问」的交互闭环。
- A3 修复 vitest 配置与 Taro mock，让前端测试变绿并纳入 CI 门禁。
- A4 连真实 LM Studio 做一次人工端到端验收，产出样例问诊记录。

### 阶段 B：让技能与工具真正生效（2–3 周）
目标：**把已经写好但没接上的能力接上**，投入小、收益大。

- B1 各 Agent 改用 `chat_with_tools`，打通「LLM → 技能 → LLM」闭环。
- B2 把单轮工具调用升级为多轮循环（可配置最大轮数），支持串行查证。
- B3 为 `treatment` 补专属技能（把 `find_formula` / `find_care` 暴露为工具）。
- B4 MCP client 接线：`config.yaml` 声明式接入外部 MCP Server。
- B5 对齐 `tcm-rag` 与 RAG 服务契约（路径 + 返回结构）。

### 阶段 C：健壮性与可观测（2–3 周）
目标：**出问题能定位，出故障不雪崩**。

- C1 每步埋点：capability / 耗时 / token / 模型 / 错误，结构化日志输出。
- C2 `/chat` 部分失败降级：返回已完成步骤 + 失败步骤的错误标记。
- C3 红旗中断契约：安全门产出结构化 `blocked` 标记，供调用方决定是否终止。
- C4 死代码清理 + `routing.yaml` 语义收敛。

### 阶段 D：诊疗质量（4–6 周）
目标：**从「能跑」到「可信」**。

- D1 结构化输出：把辨证结果从自由文本升级为 JSON（证候 + 置信度 + 支持/矛盾证据链）。
- D2 兼证呈现：当前 `infer_syndrome_slug` 已返回全部候选，但未在输出中体现。
- D3 RAG 语料建设（中医典籍 / 方剂 / 医案）与召回质量评估。
- D4 LLM 评测集：把 `cases.jsonl` 扩为「输入 → 期望证候」的自动评分集，
  定期跑分跟踪质量回归（需真实 LLM，作为 nightly 而非 PR 门禁）。
- D5 MCP Server：对外暴露 7 个能力（见 [`mcp.md`](./mcp.md) 第 2.2 节）。

### 阶段 E：生产化与上线（4–6 周）
- E1 会话/报告持久化（harness 现为无状态，需要存储层 + 可选的服务器端会话）。
- E2 对象存储（图片归档）。
- E3 前端 H5 上 CDN + 微信小程序过审。
- E4 合规审计：免责强制展示、红旗路径不可移除、日志脱敏。

### 阶段 F：家庭算力云化（按需，可与 C/D/E 并行）
- F1 rrserver 生产化：真实 TLS、强 token、随机 name、告警。
- F2 多隧道 / 多模型路由。

### 阶段 G：结论可信度（1 周，已于 2026-09-01 完成）
目标：**从「能跑」到「结论自洽」**——把人工验收发现的 4 条病症连根拔掉。

- G1 证候锁定：辨证主证注入治疗期各步，立法/用药/开方与辨证同源（T7.1）。
- G2 技能多归属：治疗期三步能拿到方剂/调护工具，不再凭记忆开方（T7.2）。
- G3 人群适配：问诊按性别过滤，并显式告知模型患者性别（T7.3）。
- G4 知识补数据 + 确定性校验：方剂库扩容到覆盖全部证候，开方输出做药味比对（T7.4）。

> 贯穿本轮的方法论：**凡是「模型记错了」，先问是不是库里没有 / 没接通**。
> 麻黄汤写错药味，是因为库里根本没有麻黄汤；开方不开本地库的方，
> 是因为开方步压根拿不到方剂工具。提示词只能约束表达，约束不了知识。

### 阶段 H：辨证可信度（1 周，已于 2026-09-02 完成）
目标：**让系统知道自己不知道**——把「置信度」从排序分改成可信的质量信号。

- H1 消除重复计分：关键词证据只计症状/舌脉之外的线索（H1）。
- H2 症状分级：主症 1.0 / 次症 0.4，凑次症压不过命中主症（H2）。
- H3 主症必备 + 孤证不立 + 库外诚实出口（H3）。
- H4 证候锁定加置信度门槛；H5 修鉴别度与强制放行可见性。
- H6 治疗期五步感知证候不确定性（含兼容档 `treatment` / `care`）。
- H7 黄金病例集：断言首位命中与库外不得出证。
- H8 证候库 6 → 16 + 配套方剂 / 关键词 / 矛盾表 / 调护。

### 阶段 J：真机验证与评测基线（已于 2026-09-03 完成）
目标：**把前面几个阶段「没看见过」的部分摆到真实环境里**。

- J1 修 H5 端白屏（模块顶层读 `process.env`）。
- J2 前端请求超时 120s → 600s（完整问诊需 200–530s）。
- J3 修提示条里的 Markdown 星号。
- J4 `llm_eval` 改用黄金病例集 + 双向评分，跑出首份基线。

> 贯穿本轮的方法论：**编译通过 ≠ 能跑，单测全绿 ≠ 页面正常**。
> 前三个阶段共产出 6 处 UI 改动，全部「编译通过、类型正确、纯函数有单测」，
> 结果第一次起 `dev:h5` 就发现**页面根本是白的**。
> 而更讽刺的是，评测那边 21 条全部 401 失败、分数为 0，
> 测试却显示「通过」——因为门槛默认就是 0。
> 两条合起来说明同一件事：**不去看一眼的检查，等于没有检查。**

### 阶段 I：残留路径与前端落地（已于 2026-09-02 完成）
目标：**把阶段 H 铺开的模式补到每一处，并让前端真的把它显示出来**。

- I1 针灸步接入辨证结论与不确定性（H6 的漏网，且针灸是有创操作）。
- I2 医案参考的检索靶点标注为「初步方向」，不再冒充结论。
- I3 前端展示 `near`：未定证时告诉用户还差哪些表现。
- I4 契约测试不再静默 skip（加了超时与 WARN，使其可观测）。

> 贯穿本轮的方法论：**改一个模式时，必须枚举所有同类调用点**。
> H6 已经「枚举」过一次（覆盖了立法/用药/开方/治疗/调护），
> 却仍漏了针灸——因为它默认不在激活档位，读代码时容易当成不存在。
> 而配置支持显式启用，用户就会踩到。教训：**枚举要按数据（治疗期有哪些步骤）来，
> 不能按印象（我记得改了哪几步）来**。

> 贯穿本轮的方法论：**先看「不知道」这条路径是否走通，再谈「知道」得准不准**。
> 阶段 G 解决了「辨证结论下传」，但没追问「这个结论本身有多可信」——
> 而 `assess()` 是在有限的证候库里**必选其一**，命中一条非特异次症就能出主证。
> 库薄只是辨不出来（诚实降级即可），**虚假置信度是错得理直气壮**：
> 报告看起来完全正常，读的人没有任何线索去怀疑它。

---

## 3. 里程碑

| 里程碑 | 交付物 | 验收 |
|---|---|---|
| **M1 端到端打通** | 页面切到 harness 契约 + 多轮交互 + 前端测试变绿 | ✅ 已达成（连 LM Studio 跑通完整问诊并归档 3 份样例；`npm run test` 32/32 全绿） |
| **M2 技能生效** | 工具调用闭环 + 多轮 + MCP 接线 + RAG 对齐 | 辨证/治疗步骤的 trace 中能看到真实工具调用 |
| **M3 可观测可降级** | 埋点 + 部分失败降级 + 红旗中断契约 | 断网/LLM 超时场景下前端仍有可读结果 |
| **M4 诊疗提质** | 结构化辨证输出 + 证据链 + 兼证 + LLM 评测集 + RAG 语料 + MCP Server | ✅ 已达成（LLM 评测基线待接真实 LLM 后出分） |
| **M5 可上线** | 持久化 + 对象存储 + 小程序发布 + 合规 | 持久化与合规 ✅，对象存储 / 小程序受外部资源阻塞 |
| **M6 结论可信** | 证候锁定 + 治疗期工具 + 人群适配 + 方剂库与药味校验 | ✅ 已达成（人工验收的 4 条结论质量问题全部修复，后端 193 用例全绿） |
| **M7 辨证可信** | 症状分级 + 主症必备 + 库外诚实出口 + 锁定门槛 + 可信度可见 + 黄金病例集 | ✅ 已达成（库内与库外两条路径均经真实 LLM 复验，后端 212 用例全绿） |

依赖关系：`M1 → M2 → M3`（串行，逐级加固）；`M4` 可在 `M2` 后并行启动；
`M5` 依赖 `M3`；`M6` 依赖 `M2`（治疗期工具）与 `M4`（结构化辨证，锁定要有可锁的东西）；
`M7` 依赖 `M4`（结构化辨证，要分级就得先有结构）与 `M6`（证候锁定，要有可加门槛的东西）；
`F` 阶段完全独立。

---

## 4. 风险与待确认

1. **模型可得性**：推理完全依赖宿主机 LM Studio。LM Studio 未启动时
   `/chat` 直接报错（无 mock 兜底），演示与验收前必须确认模型已加载。
2. **医疗合规**：AI 健康参考，非医疗诊断。上线前需法务/合规复核免责声明、
   红旗路径与日志脱敏。
3. **LLM 评测基线尚未建立**：nightly 评测集（D4）已就绪，但需在 self-hosted runner
   上连真实 LLM 跑一次以确立基线分，`HARNESS_EVAL_MIN_SCORE` 才能在 CI 中真正设门槛。
4. **知识库权威性**：方剂/剂量必须由专业中医复核，`cases.jsonl` 只保证数据存在性，
   不保证正确性。
5. **磁盘**：`server/target/` 常达数 GB，可安全删除（见 [`cleanup.md`](./cleanup.md)）。

---

## 5. 明确不做（避免过度设计）

- **不在 harness 内实现服务端会话**：先由前端维护 `messages`；确有续诊/报告留存需求时
  再在 E1 引入独立存储层。
- **不自建模型服务**：推理统一交给 LM Studio / 兼容端点。
- **不重复造 llm_server 已有的能力**：prompt 优化、多轮 agent 循环、MCP 聚合
  都在 `llm_server`；harness 侧只保留「够用的单/多轮工具调用」。
- **不做多租户 / 权限体系**：当前为单机自用形态。

---

## 6. 如何参与

- 任务看板与验收标准：[`tasks.md`](./tasks.md)
- 新增 Sub-Agent：[`agent-protocol.md`](./agent-protocol.md)
- 新增技能：[`skills.md`](./skills.md)
- 本地联调：起 harness 容器（`docker run` 见 `deployment.md` 3.1）
  + `cd frontend && npm run dev:h5`
- 测试：后端在 Docker 内 `cargo test --workspace`（见 `testing.md`）；
  全链路 `cd e2e_tests && .\run_full_chain_e2e.ps1`
