[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    e2e: 端到端测试，启动真实 uvicorn 服务并用 httpx 直连（tests/e2e）
addopts = -q --import-mode=importlib
filterwarnings =
    ignore::DeprecationWarning
